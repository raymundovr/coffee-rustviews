{
    "gitlab": {
        "base_url": "https://gitlab.com/api/v4", /* Required. Specify your own instance URL if necessary. */
        // "include_wip": true, /* Optional. Include this if you want to include WIP Merge Requests */
        // "projects": ["project_id"], /* Optional. Include only Merge Requests of the specified projects */
        "token": "" /* Required. Your API Token */
    },
    "publish": {
        "slack": { /* Optional. Set Slack configuration if needed */
            "webhook_url": "https://hooks.slack.com/yourappwebhook-url" 
        },
        "teams": { /* Optional. Set MS Teams configuration if needed */
            "webhook_url": "https://domain.webhook.office.com/yourwebhookconnector-url"
        }
    }
}
